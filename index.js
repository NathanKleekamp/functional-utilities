const {
  rest,
  first,
  last,
} = require('./array');
const {
  isNumber,
  isNotNumber,
  isEven,
  isOdd,
} = require('./number');
const {
  complement,
  compose,
} = require('./function');

module.exports = {
  isNumber,
  isNotNumber,
  isEven,
  isOdd,
  rest,
  first,
  last,
  complement,
  compose,
};
